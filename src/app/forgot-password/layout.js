export const metadata = {
  title: 'Forgot Password',
  description: 'Reset your password for Virudhunagar Employment Connect.',
  robots: {
    index: false,
    follow: true,
  },
};

export default function ForgotPasswordLayout({ children }) {
  return children;
}
