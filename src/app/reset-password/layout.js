export const metadata = {
  title: 'Reset Password',
  description: 'Reset your password for Virudhunagar Employment Connect.',
  robots: {
    index: false, // Do not index reset password pages
    follow: false,
  },
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
