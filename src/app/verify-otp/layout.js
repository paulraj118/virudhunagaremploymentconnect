export const metadata = {
  title: 'Verify OTP',
  description: 'Verify your OTP for Virudhunagar Employment Connect.',
  robots: {
    index: false, // Do not index verify OTP pages
    follow: false,
  },
};

export default function VerifyOtpLayout({ children }) {
  return children;
}
